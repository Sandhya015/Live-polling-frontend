{
  "name": "live-polling-system",
  "version": "1.0.0",
  "private": true,
  "description": "Live Polling System - monorepo root",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
